import * as React from "react";
import * as ReactDOM from "react-dom";
import { TransitionExperiment } from './transition-experiment';

// ReactDOM.render(React.createElement(App), document.querySelector("#react-app"));


ReactDOM.render(React.createElement(TransitionExperiment), document.querySelector("#react-app"));
